# OpenSCAD MCP server: separate clone with its own .git (see README setup)
tools/OpenSCAD-MCP-Server/

# Scratch preview renders; export/pieces/ and export/plates/ are committed
# so people can print without running the pipeline
export/*.png
*.gcode

# Raw geodata downloads (regenerable via scripts)
data/raw/

# OS noise
.DS_Store
__pycache__/
*.pyc
.venv/
